seed: 7
run_dir: runs/local_smoke

data:
  num_scenes: 8
  history_steps: 8
  future_steps: 12
  num_agents: 8
  num_candidates: 64
  dt: 0.2

model:
  hidden_dim: 128

smoke:
  batch_size: 4
  device: auto
